{
  "name": "bas-atlas",
  "version": "0.1.0",
  "description": "BAS Atlas - community-driven equipment database for BAS professionals",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:clean": "node scripts/build.mjs --clean",
    "validate": "node scripts/validate.mjs",
    "test": "node scripts/test.mjs",
    "check": "npm run build:clean && npm run validate && npm run test"
  },
  "license": "MIT",
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
