name: Build Validate Test

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "data/**"
      - "scripts/**"
      - "src/**"
      - "schemas/**"
      - "tests/**"
      - "package.json"
      - "package-lock.json"
      - ".github/workflows/**"
  pull_request:
    branches: [main]
    paths:
      - "data/**"
      - "scripts/**"
      - "src/**"
      - "schemas/**"
      - "tests/**"
      - "package.json"
      - "package-lock.json"
      - ".github/workflows/**"

jobs:
  verify:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Build clean
        run: npm run build:clean

      - name: Validate schemas
        run: npm run validate

      - name: Run tests
        run: npm run test

      - name: Ensure expected artifacts
        run: |
          test -f dist/index.json
          test -f dist/categories.json
          test -f dist/search-index.json
          test -f dist/templates.json
          test -f dist/graph.json
